/**
 * 1. install stripe and stripe react
 * 2. create card element
 * 3. create stripe account and use the publishable key(PK) in the stripePromise variable
 * 4. create checkout form and get card information and error
 * 5. create payment intent post on the server side and return client secret. Install
 stripe on the server side and get client secret and make sure you used the 
 payment_method_types: ['card', 'link']
 * 6. from client side get the client secret and save it.
 * 7. use confirm card payment and pass user information, card and client secret
 * 8. display transaction id
 *  
 * 
 */
